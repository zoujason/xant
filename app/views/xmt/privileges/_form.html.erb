<%= form_for([:xmt, mgr_privilege]) do |f| %>  <% if mgr_privilege.errors.any? %>    <div id="error_explanation">      <h2><%= pluralize(mgr_privilege.errors.count, "error") %> prohibited this mgr_privilege from being saved:</h2>      <ul>      <% mgr_privilege.errors.full_messages.each do |message| %>        <li><%= message %></li>      <% end %>      </ul>    </div>  <% end %>    <div class="field">      <%= f.label :role %>      <%= f.collection_select :role_id, Role.all, :id, :name %>    </div>    <div class="field">      <%= f.label :operation %>      <%#= f.collection_select :operation_id, Operation.all, :id, :name %>      <%= f.check_box :operation_id,{:checked=>false}, "编辑权限" %>编辑      <%= f.check_box :operation_id,{:checked=>false}, "删除权限" %>删除    </div>  <div class="actions">    <%= f.submit %>  </div><% end %>