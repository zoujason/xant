<%= form_for(mgr_menu_access) do |f| %>
  <% if mgr_menu_access.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mgr_menu_access.errors.count, "error") %> prohibited this mgr_menu_access from being saved:</h2>

      <ul>
      <% mgr_menu_access.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
