<%= form_for(mgr_home) do |f| %>
  <% if mgr_home.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mgr_home.errors.count, "error") %> prohibited this mgr_home from being saved:</h2>

      <ul>
      <% mgr_home.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
