<%= form_for(mgr_document) do |f| %>
  <% if mgr_document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mgr_document.errors.count, "error") %> prohibited this mgr_document from being saved:</h2>

      <ul>
      <% mgr_document.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
