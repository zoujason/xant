<%= form_for(mgr_image) do |f| %>
  <% if mgr_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mgr_image.errors.count, "error") %> prohibited this mgr_image from being saved:</h2>

      <ul>
      <% mgr_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
