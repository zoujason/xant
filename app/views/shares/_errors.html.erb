<div id="error_explanation">
      <h2><%= pluralize(obj.errors.count, "条错误信息") %> </h2>

      <ul>
        <% obj.errors.full_messages.each do |message| %>
            <li><%= message %></li>
        <% end %>
      </ul>
</div>
